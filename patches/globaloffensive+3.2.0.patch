diff --git a/node_modules/globaloffensive/handlers.js b/node_modules/globaloffensive/handlers.js
index 595cb86..88c0531 100644
--- a/node_modules/globaloffensive/handlers.js
+++ b/node_modules/globaloffensive/handlers.js
@@ -1,4 +1,3 @@
-const ByteBuffer = require('bytebuffer');
 const Long = require('long');
 const SteamID = require('steamid');
 
@@ -358,7 +357,7 @@ handlers[Language.SO_UpdateMultiple] = function(body) {
 };
 
 function decodeProto(proto, encoded) {
-	if (ByteBuffer.isByteBuffer(encoded)) {
+	if (Buffer.isBuffer(encoded)) {
 		encoded = encoded.toBuffer();
 	}
 
diff --git a/node_modules/globaloffensive/index.js b/node_modules/globaloffensive/index.js
index 2c54039..eef6b3d 100644
--- a/node_modules/globaloffensive/index.js
+++ b/node_modules/globaloffensive/index.js
@@ -1,4 +1,3 @@
-const ByteBuffer = require('bytebuffer');
 const EventEmitter = require('events').EventEmitter;
 const {ShareCode} = require('globaloffensive-sharecode');
 const SteamID = require('steamid');
@@ -49,7 +48,7 @@ function GlobalOffensive(steam) {
 
 		this.emit('debug', "Got " + (handler ? "handled" : "unhandled") + " GC message " + msgName + (isProtobuf ? " (protobuf)" : ""));
 		if (handler) {
-			handler.call(this, isProtobuf ? payload : ByteBuffer.wrap(payload, ByteBuffer.LITTLE_ENDIAN));
+			handler.call(this, isProtobuf ? payload : payload); // Pass payload directly		
 		}
 	});
 
